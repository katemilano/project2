let maxResults = 0;
let counter = 0;

const showResults = (results) => {

  const show = () => {
    // 0 = 0; 5 = 0;
    counter = maxResults;
    // 0 = 0, 0 < 13 && 0 < 5, 0++;
    for (maxResults = counter; counter < options && counter < maxResults + 5; counter++) {

    };
  };
};